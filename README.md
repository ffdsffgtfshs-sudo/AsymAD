# AsymAD
Official implementation of the paper:AsymAD_Asymmetric Student-Teacher Network with Dual-Perturbation for Precise Multi-class Anomaly Detection
## 1. Environments
 1. Environments

Create a new conda environment and install required packages.

```bash
conda create -n asymad python=3.8
conda activate asymad
pip install -r requirements.txt
Experiments are conducted on NVIDIA GeForce RTX 3090 (24GB). Same or higher GPU specifications are recommended.
